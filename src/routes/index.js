const authRouter = require('./auth.router');
const userRouter = require('./user.router');
const categorieRouter = require('./categorie.router');

module.exports = { authRouter, userRouter, categorieRouter };